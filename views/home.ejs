<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSMSS FACULTY ACCESS</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .container-custom {
            margin-top: 50px;
        }
        .custom-card {
            border-radius: 15px;
            background-color: white;
            padding: 30px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }
        .custom-card-header {
            background-color: #007bff;
            color: white;
            border-radius: 15px 15px 0 0;
            padding: 10px;
        }
        .custom-card-body {
            padding: 20px;
        }
        .btn-custom {
            background-color: #007bff;
            color: white;
        }
        .info-box {
            margin-top: 30px;
            background-color: #f8f9fa;
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 20px;
        }
        .info-box p {
            margin: 0;
            font-size: 1rem;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container text-center container-custom">
        <div class="card custom-card">
            <div class="card-header custom-card-header">
                <h3>CSMSS Faculty Access</h3>
            </div>
            <div class="card-body custom-card-body">
                <div class="row justify-content-center">
                    <!-- Download Format Button -->
                    <div class="col-md-5 mb-4">
                        <h4>Download Excel Format</h4>
                        <a href="https://drive.google.com/uc?export=download&id=1oNsykIn1VRugGjEmAwt-IFILvTOw2VhL" class="btn btn-custom btn-lg" target="_blank">
                            Download Format
                        </a>
                    </div>
                    <!-- File Upload Form -->
                    <div class="col-md-5">
                        <h4>Upload Excel File</h4>
                        <form action="/upload-excel" method="POST" enctype="multipart/form-data">
                            <div class="mb-3">
                                <input type="file" class="form-control" name="file" accept=".xlsx, .xls" required>
                            </div>
                            <button type="submit" class="btn btn-custom btn-lg">Upload</button>
                        </form>
                    </div>
                </div>
                <!-- Info Box -->
                <div class="info-box">
                    <p><strong>Note:</strong> While uploading the Excel sheet, ensure that your file is in the correct Excel format and that the data in the file adheres to the provided format.</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
